type Query {
  allUsers: [User!]!
}

type Mutation {
  createAccount(
    name: String!
    email: String!
    avatar: String
    bio: String
  ): Boolean!
  editUser(
    avatar: String
    firstName: String
    lastName: String
    bio: String
  ): Boolean!
}
